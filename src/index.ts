import {
    Tag,
    TagVoid,
    TextNode,
    IBaseNode,
    Attribute,
    nodeP,
    documentP,
} from "./HTML";

import {
    div
} from "./HTML-Shorthand";

import Parser, { TParser } from "./Parser";

export {
    Tag,
    TagVoid,
    TextNode,
    IBaseNode,
    Attribute,
    nodeP,
    documentP,
    div,
    Parser,
    TParser,
}
